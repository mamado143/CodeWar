public class Move {
    public static int move(int position, int roll) {
      // Double the roll and add it to the current position
        position +=  + (roll * 2);
      return position;
        
        
    }
}